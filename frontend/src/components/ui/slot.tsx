import { JSX } from "solid-js/jsx-runtime";

const SlotRoot = (props: { children: JSX.Element }) => {
  return props.children;
};

const SlotClone = (props: { children: JSX.Element }) => {
  return props.children;
};
